# SIGIL — pinned application install lockfile (RT-2026-05-01-009).
#
# This file pins exact versions of every direct and transitive dependency
# used at the time the lockfile was generated. Use it for reproducible
# application installs:
#
#     pip install --no-deps -r requirements-lock.txt
#
# pyproject.toml keeps loose ranges so the project remains usable as a
# library; this file is the deliberate-upgrade snapshot for shipped
# binaries / containers / CI runs. Regenerate by:
#
#     python -m pip freeze > requirements-lock.txt
#
# and review every changed line before committing.

# --- Direct runtime dependencies ---
PyNaCl==1.6.2
httpx==0.28.1
python-dotenv==1.2.2
tiktoken==0.12.0

# --- Transitive dependencies (pulled in by the above) ---
anyio==4.13.0
certifi==2026.2.25
cffi==2.0.0
charset-normalizer==3.4.6
cryptography==46.0.6
h11==0.16.0
httpcore==1.0.9
idna==3.11
pycparser==3.0
regex==2026.3.32
requests==2.33.1
sniffio==1.3.1
typing-extensions==4.15.0
urllib3==2.6.3

# --- Development / test dependencies ---
pytest==9.0.2
# pytest's transitives
colorama==0.4.6
iniconfig==2.3.0
packaging==26.2
pluggy==1.6.0
Pygments==2.20.0
